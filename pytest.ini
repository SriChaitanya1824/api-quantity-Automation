[pytest]
testpaths = tests
addopts = -ra --strict-markers
pythonpath =
    backend
    .
markers =
    api: REST API tests
    ui: Playwright browser tests
    smoke: Critical smoke tests
    regression: Regression test suite
    performance: Locust or performance smoke tests
